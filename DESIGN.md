# Design

The Python package is the canonical reference implementation for OMST v0.1. JSON Schema defines interchange boundaries, while the Python models implement validation, routing, analytics and deterministic simulations.

The router defaults to lowest transition degradation subject to hard transaction constraints. It does not optimise purely for cost.
