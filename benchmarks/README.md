# Benchmarks

Planned benchmarks:

- schema validation speed
- state transition evaluation
- route discovery
- graph traversal
- simulation throughput

Benchmarks must be deterministic and must not depend on private or live market data by default.
