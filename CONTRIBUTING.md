# Contributing

Use issues for proposals and RFC-style changes. Keep examples synthetic unless every material claim is backed by public primary sources. Run `ruff check .`, `mypy src`, and `pytest` before submitting changes.
