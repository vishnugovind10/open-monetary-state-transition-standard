# Generic Adapter

The generic adapter defines the minimum OMST adapter contract:

- identify source and target representation
- list supported objects
- classify every mapped field
- preserve unsupported fields as explicit `UNSUPPORTED` mappings
- declare lossiness and constraints

This adapter is a reference pattern, not an external integration.
