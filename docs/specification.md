# OMST Specification

OMST defines machine-readable primitives for describing, validating, simulating and analysing state transitions of digital money across tokenised financial systems.

v0.3 adds settlement intent, settlement compatibility, transition plans, settlement bundles, finality vocabulary, availability vocabulary and executable stress output.

See the repository root `SPECIFICATION.md` for the complete specification.
