# RFC-0032 Evaluation Package

Status: Draft

Defines `evaluation-package` as the portable unit for independently verifying a settlement compatibility result.

An evaluation package contains canonical source references, canonical evaluation output, evidence manifest, ruleset version, schema version, canonicalization profile and integrity fingerprints.
