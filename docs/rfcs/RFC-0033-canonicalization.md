# RFC-0033 Canonicalization

Status: Draft

Defines `OMST-CANONICAL-JSON-0.7` as the canonical JSON profile used by v0.7 verification packages.

Implementations must produce deterministic JSON hashes for semantically equivalent package content and must not let formatting differences alter semantic verification outcomes.
